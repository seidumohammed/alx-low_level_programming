#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */

int main(void)
{
	int i;
	int k;

	for (i = 48; i < 58; i++)
	{
		for (k = 48; k < 58; k++)
		{
			if (i != j && i < j)
			{
				putchar(i);
				putchar(k);
				if (k == 57 && i == 56)
				{
					break
						;
				}
				putchar(',');
				putchar(' ');
			}
		}
	}
	putchar('\n');
	return (0);
}
