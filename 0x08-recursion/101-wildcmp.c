#include <stdio.h>
#include "main.h"
/**
 * wildcmp - compares two strings, returns 1 if identical and 0 otherwise
 * @s1: input
 * @s2: input
 * Return: 0
 */
int wildcmp(char *s1, char *s2)
{
	return (0);
}
